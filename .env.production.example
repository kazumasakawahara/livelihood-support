# =============================================================================
# Production Environment Variables
# 生活保護受給者尊厳支援システム - 本番環境設定
# =============================================================================
#
# 使用方法:
#   1. このファイルを .env.production としてコピー
#   2. 各値を本番環境に合わせて設定
#   3. ファイルのパーミッションを制限: chmod 600 .env.production
#
# =============================================================================

# -----------------------------------------------------------------------------
# Neo4j Database
# -----------------------------------------------------------------------------
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE

# メモリ設定（サーバースペックに応じて調整）
NEO4J_PAGECACHE_SIZE=2G
NEO4J_HEAP_SIZE=2G

# -----------------------------------------------------------------------------
# Keycloak Authentication
# -----------------------------------------------------------------------------
KEYCLOAK_URL=https://auth.your-domain.example.com
KEYCLOAK_REALM=livelihood-support
KEYCLOAK_CLIENT_ID=livelihood-support-app
KEYCLOAK_API_CLIENT_SECRET=CHANGE_ME_API_SECRET_HERE

# Keycloak管理者（初回セットアップ用）
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=CHANGE_ME_ADMIN_PASSWORD_HERE
KEYCLOAK_DB_PASSWORD=CHANGE_ME_DB_PASSWORD_HERE
KEYCLOAK_HOSTNAME=auth.your-domain.example.com

# -----------------------------------------------------------------------------
# API Settings
# -----------------------------------------------------------------------------
# 本番環境では必ず false に設定
DEBUG=false
AUTH_SKIP=false
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# Gemini API (AI機能)
# -----------------------------------------------------------------------------
GEMINI_API_KEY=your-gemini-api-key-here

# -----------------------------------------------------------------------------
# Monitoring
# -----------------------------------------------------------------------------
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=CHANGE_ME_GRAFANA_PASSWORD_HERE

# -----------------------------------------------------------------------------
# Domain & SSL
# -----------------------------------------------------------------------------
DOMAIN=your-domain.example.com
HTTP_PORT=80
HTTPS_PORT=443

# -----------------------------------------------------------------------------
# Backup Settings
# -----------------------------------------------------------------------------
BACKUP_RETENTION_DAYS=30
BACKUP_S3_BUCKET=your-backup-bucket
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=ap-northeast-1

# -----------------------------------------------------------------------------
# Email Notifications (オプション)
# -----------------------------------------------------------------------------
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=alerts@your-domain.example.com
SMTP_PASSWORD=CHANGE_ME_SMTP_PASSWORD_HERE
ALERT_EMAIL=admin@your-domain.example.com

# =============================================================================
# Security Checklist (デプロイ前に確認)
# =============================================================================
# [ ] すべてのパスワードを強力なものに変更
# [ ] DEBUG=false を確認
# [ ] AUTH_SKIP=false を確認
# [ ] SSL証明書を設定
# [ ] ファイアウォールで必要なポートのみ開放
# [ ] バックアップ設定を確認
# [ ] 監視アラートを設定
# [ ] ログローテーションを設定
# =============================================================================
